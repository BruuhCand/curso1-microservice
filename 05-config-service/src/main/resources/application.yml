server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BruuhCand/curso1-config-server
          default-label: main
          search-paths:
          - 'greeting-service*'